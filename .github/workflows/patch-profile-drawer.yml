name: One-time profile drawer patch

on:
  push:
    paths:
      - '.github/workflows/patch-profile-drawer.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch profile and drawer
        run: python3 scripts/patch_profile_drawer.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/de/bascurt/almancaokuyucu/MainActivity.kt
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Unify profile entry and profile view"
            git push
          fi
